import React from 'react'
import { LOGIN_PATH ,SIGNIN_PATH} from './index'
describe('Constants', () => {
   it('should test Login Path', () => {
      expect(LOGIN_PATH).toBeDefined()
   })
      it('should test Login Path', () => {
         expect(SIGNIN_PATH).toBeDefined()
      })
})